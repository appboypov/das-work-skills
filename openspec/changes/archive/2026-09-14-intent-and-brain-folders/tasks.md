## 1. Confirmed intent context

- [x] 1.1 In `das-work-skills`, update setup and the canonical workflow to configure one intent folder and one brain folder, read destination instructions and resolve paths from the defining `.das-work`. Prove configured placement from a different working directory.
- [x] 1.2 Connect exploration, grilling, specification authoring, proposal schema/template, handoff, Skuddy and onboarding to confirmed intent discovery and capture. Align the bundled and repository-local schema copies. Prove active-intent reuse, independent creation, corrections before writes, advice-only behaviour and framework opt-out. Depends on 1.1.
- [x] 1.3 Prove that continuation across repositories retains the intent and defining context, and that archiving one linked change leaves unfinished intent work active. Depends on 1.2 and 2.2 for the archive case.

## 2. Permanent archive knowledge

- [x] 2.1 Add the shared brain-ingestion reference with instruction-led source gathering, curated records with sources, record reconciliation and accurate outcome claims. Exercise default preservation and a brain-specific format with existing destination records. Depends on 1.1.
- [x] 2.2 Integrate single archiving with preflight, ingestion, readback and saved output paths. Prove an inaccessible required source blocks the move and a successful archive preserves the relevant intent, change, evidence and linked issue material. Depends on 1.2 and 2.1.
- [x] 2.3 Integrate archived-change resumption and bulk per-change outcomes through the shared procedure. Resolve conflicting batch failure instructions and the empty-active-change exit. Prove post-move failure, resumption without duplicate records, a shared preflight blocker and independent continuation after a change-specific failure. Depends on 2.2.

## 3. Delivery guidance and verification

- [x] 3.1 Update README and acceptance journeys to cover the complete configured intent and brain workflow, including the authorization boundaries and skill-driven archive scope. Check every scenario against the two capability deltas. Depends on sections 1 and 2.
- [x] 3.2 Run repository checks, validate both schema copies and exercise the agreed journeys through installed skills in disposable folders. Record commands, saved results and any live Linear access limitation in `evidence.md`; preserve historical verification claims. Review affected guidance for contradictions, duplicated rules and personal-only dependencies. Depends on 3.1.

## Linear links

No related issue has been established. All tasks belong to `das-work-skills`; this planning request does not authorize creating Linear issues. Implementation approval and the apply boundary are recorded in [design](design.md#implementation-approval).
